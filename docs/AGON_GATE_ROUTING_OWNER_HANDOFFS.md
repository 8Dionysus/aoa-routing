# Agon Gate Routing Owner Handoffs

Wave V keeps routing thin by naming owners instead of copying their authority.

## Owner map

`Agents-of-Abyss` owns Agon law, lawful move vocabulary, future arena session model, contradiction/closure/summon law, and terminal outcome law.

`aoa-agents` owns actor form, kind, office, eligibility, assistant exclusion, and formation readiness.

`aoa-evals` owns proof, breach, closure legality checks, and later verdict-support surfaces.

`aoa-memo` owns future scar memory, delta history, and retention memory.

`aoa-playbooks` owns trial choreography.

`aoa-stats` owns derived movement summaries.

`Tree-of-Sophia` owns slow canon and must not receive direct arena writes.

## Routing promise

`aoa-routing` may point to these owners.

It must not become them.
