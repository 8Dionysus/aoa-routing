# Local Eval Port

This skeleton `evals/` port will preserve routing-born eval pressure such as
route selection cases, two-stage router precision cases, and handoff hint
failures.

`aoa-evals` owns central verdict, scoring, regression, and proof doctrine
authority. Routing surfaces here may preserve local cases, suites, reports, and
intake packets only.
