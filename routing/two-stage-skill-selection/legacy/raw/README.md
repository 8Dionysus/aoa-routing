# Raw Legacy

No raw file copy is needed for this source-home move. The old flat files were
renamed into active branch paths, and `former-routes.json` records the mapping.

If a future migration preserves non-active source text, place it here with an
index entry. Do not import from this directory.
