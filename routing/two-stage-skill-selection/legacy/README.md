# Two-Stage Skill Selection Legacy

This directory records the old flat addresses retired by the source-home move.

Legacy here means provenance and old-path accounting. It does not mean a second
active implementation, fallback import path, or compatibility source.

Active files live in `routing/two-stage-skill-selection/`. Root scripts may
remain as compatibility wrappers only.
