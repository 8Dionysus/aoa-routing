# Routing Core

This retired source-home route preserves predecessor thin-router contracts and
local routing behavior as historical evidence. It owns no active changes.
Route every fix, feature, ABI, generated-output, and publication request to
`aoa-sdk`. The schemas under `schemas/` are preserved predecessor contracts;
root `schemas/` is only a former flat path.
