# Return Navigation

Owns the return-navigation boundary doc and schema contract.

Active payloads:

- `docs/recurrence-navigation-boundary.md`
- `schemas/return-navigation-hints.schema.json`

The generated public output stays at `generated/return_navigation_hints.min.json`
because it is a router read model.
