# AGENTS.md

Checkpoint parts are operation nodes.

Each part should keep its own docs, schemas, examples, generated companions,
scripts, and tests when those files belong to that operation.

Do not create file-type buckets under `parts/`.
