# Antifragility Mechanics

This package owns aoa-routing antifragility parts only when the route operation
stays thin, evidence-aware, and source-first.

Use `parts/<part>/` for active docs, schemas, examples, and tests. Use
`legacy/` only to account for former flat paths.
